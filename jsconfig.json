{
  "compilerOptions": {
    "module": "nodenext",
    "moduleResolution": "nodenext",

    // 设定路径解析的基准目录
    "baseUrl": ".",

    // 配置别名，只匹配 "@/..." 的导入
    "paths": {
      "@/*": ["src/*"],
      "~utils/*": ["src/utils/*"]
    }
  },

  // 只包含 src 下的源码，不去扫描 node_modules
  "include": ["src", "__tests__"],
  "exclude": ["node_modules"]
}
